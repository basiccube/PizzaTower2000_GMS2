with (obj_wall)
{
    if (object_index == obj_wall)
        visible = other.showcollisions
}
with (obj_platform)
    visible = other.showcollisions
with (obj_slope)
    visible = other.showcollisions
