if (!obj_uibar.active && (obj_editor.mode == 2 || obj_editor.mode == 3) && !place_meeting(mouse_x, mouse_y, obj_scaletoolbar) && obj_editor.uibuffer <= 0)
{
    with (obj_object)
        selected = false
    selected = true
}
